---
# file: wordpress/defaults/main.yml

wordpress_dir_credentials: "{{ inventory_dir }}/_private/credentials"
wordpress_dir_www_root: /srv/www
wordpress_user_www: www-data
wordpress_group_www: www-data
wordpress_dir_mode: '0755'
wordpress_file_mode: '0644'
wordpress_default_restore_saved_db: false
wordpress_default_db_host: localhost

# If we want to move the app files down a level
# and other site folder (eg. /logs)
wordpress_dir_structure:
  base:  ''
# logs:  '/logs'
# site:  '/site'


# Example Site Configuration
wordpress_sites:
  - domain: example.test
    alt_domains:
      - www.example.text
    repository: git@example.com:user/repo.git
    #restore_saved_db: false
    site_install: true
    site_title: Example Site
    admin_user: admin
    admin_password: admin
    admin_email: admin@example.dev
    system_cron: true
    run_composer: true
    multisite:
      enabled: false
    #home: http://example.dev
    #siteurl: http://example.dev/wp
    #env: development
    #db_host: localhost
    #db_name: example_dev
    #db_user: example_dbuser
    #db_password: example_dbpassword
    #auth_key:
    #secure_auth_key:
    #AUTH_KEY:
    #SECURE_AUTH_KEY:
    #LOGGED_IN_KEY:
    #NONCE_KEY:
    #AUTH_SALT:
    #SECURE_AUTH_SALT:
    #LOGGED_IN_SALT:
    #NONCE_SALT:
